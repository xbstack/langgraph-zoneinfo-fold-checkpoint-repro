# Fix status

No upstream fix is claimed here as of 2026-09-07.

This directory is intentionally documentation-only until XBSTACK verifies a merged/released LangGraph change that preserves both the IANA timezone identity and the `fold` bit across checkpoint serialization. A future fixed fixture should be added only after testing an actual upstream release or commit.
